# M-INFRA-HYGIENE-1 — keep the build context lean: never ship VCS
# metadata, byte-code caches, tool caches or tests into the image layer
# or the build cache. Build inputs (requirements*.txt/.lock, *.py
# sources, docker/, config yaml) are deliberately NOT excluded.
.git/
.gitignore
.dockerignore
**/__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
test_*.py
*_test.py
# A .dockerignore pattern is matched against the whole path, so `test_*.py`
# alone leaves `tests/test_server.py` in the context.
tests/
requirements-dev.txt
conftest.py
README.md
