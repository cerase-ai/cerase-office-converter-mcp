# cerase-office-converter MCP — Python dependency lock (OPT-15).
#
# Versions chosen to match what the Dockerfile previously installed
# unpinned + the existing version ranges declared inline.
mcp>=1.28.1,<2.0
mcp-proxy>=0.12.0,<1.0
